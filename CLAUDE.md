# CLAUDE.md

This project uses `AGENTS.md` as the primary instruction file for all AI agents.

**Start here:**
1. Read `AGENTS.md` — working rules, structure, src/ policy
2. Read `memory/STATUS.md` — current project state
3. Run `npx mulch-cli prime` — load structured expertise

All automation source code and scripts go in `src/`.
